<%= form_for(@list) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %><br/></br/>
    <%= f.label :color %><br />
    <input type="color" id="list_color" name="list[color]" value="<%= @list.color %>" /><br/>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
